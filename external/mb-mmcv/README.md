# mb-mmcv

A stripped-down version of mmcv, which is compatible with latest pytorch versions and has minimal dependencies.

## Build

Building wheels can be executed with the following script:

```bash
bash build_wheel.shW
```

Note:
- The build script assumes CUDA 12.8 to be installed on the build machine.
